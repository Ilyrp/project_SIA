package controller;
import view.Login;
import model.ModelAdmin;
        
public class Menu {
    
    // public void userLogin(){
    // String username = input.getUserInput("Username: ");
    // String password = input.getUserInput("Password");  
    // }

    // public boolean validate(String username, String password) {
    //     List<ModelAdmin> adminList = ModelAdmin.getAdminList(); // Assuming ModelAdmin has a static method getAdminList() that returns the existing ArrayList of ModelAdmin objects
        
    //     for (ModelAdmin admin : adminList) {
    //         if (admin.getUsername().equals(username) && admin.getPassword().equals(password)) {
    //             return true;
    //         }
    //     }
        
    //     return false;
    // }
}
